# ============================================================================
# Fooocus 2.5.6 - Colab Edition
# Auditado contra el runtime actual de Google Colab:
#   Python 3.13 | torch >= 2.6 (CUDA 12.8/13) | numpy 2.x
#
# Reglas de este archivo (no romperlas a la ligera):
#   1. torch y torchvision NO se listan aqui. El runtime ya trae un build con
#      CUDA; reinstalarlos es la causa numero uno de "Torch not compiled with
#      CUDA enabled", y ademas no existen wheels de torch 2.1 para Python 3.13.
#   2. numpy NO se fija a 1.x: no hay wheels de numpy 1.x para Python 3.13.
#      El codigo de Fooocus es compatible con numpy 2 (auditado archivo por
#      archivo) y modules/compat.py cubre a las dependencias que no lo son.
#   3. gradio se instala aparte con --no-deps (ver launch.py): su metadata pide
#      numpy~=1.0 y pillow<11, imposibles aqui. Sus dependencias reales estan
#      listadas abajo a mano.
# ============================================================================

# --- nucleo de difusion ---
torchsde>=0.2.6
einops>=0.8.0
safetensors>=0.4.3
accelerate>=0.32.1
omegaconf>=2.3.0
pyyaml>=6.0.1
pillow>=10.4.0
tqdm>=4.66.4
psutil>=6.0.0
packaging>=24.1
numpy>=1.26,<3
timm>=1.0.7
onnxruntime>=1.20.0
pygit2>=1.15.0

# --- text encoders y prompt expansion ---
# 4.49 es la ultima rama que conserva las APIs internas que Fooocus parchea en
# modules/patch_clip.py (modeling_utils.no_init_weights, CLIPTextModel,
# CLIPVisionModelWithProjection). transformers 5.x las cambia y rompe el CLIP.
transformers>=4.49.0,<4.50.0
tokenizers>=0.21,<0.22
# huggingface_hub 1.x elimina SpaceStage, que gradio_client 0.5.0 importa al cargar.
huggingface_hub>=0.26.0,<1.0

# --- stack web de gradio 3.41.2 ---
# starlette 0.37.2 es la ultima que acepta TemplateResponse(name, context), la firma
# que usa gradio 3 y que Fooocus parchea en modules/ui_gradio_extensions.py.
fastapi>=0.112.0,<0.113.0
starlette>=0.37.2,<0.38.0
pydantic>=2.9,<2.10
# 0.0.9 es la ultima que expone el modulo `multipart` que importa starlette 0.37
# (sin el, fallan todas las subidas de imagenes de la UI).
python-multipart==0.0.9
# websockets 15 elimina websockets.legacy, que gradio_client 0.5.0 importa arriba.
websockets>=13.0,<15.0
httpx>=0.27,<0.29
uvicorn>=0.24
aiofiles>=22.0
altair>=4.2,<6
orjson>=3.9
semantic-version>=2.0
importlib-resources>=1.3
ffmpy
pydub
